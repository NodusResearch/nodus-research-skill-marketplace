Describe when to use this skill, required inputs, the method, expected output and limitations.
Respect the user request, cite supplied sources accurately and distinguish assumptions from evidence.

Call `summarize` for arithmetic over numbers the user supplied. Call the `example-capability`
tool `convert` when the user asks for a unit conversion. Never state a converted value you
have not received from the capability.
