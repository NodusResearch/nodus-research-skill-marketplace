// Generated at build time from skills/chemistry-studio/SKILL.md, so the repair prompts
// quote exactly the rules the model was given. The value below is a placeholder the
// typechecker and the tests use; the build replaces it.
export const CHEMISTRY_INSTRUCTIONS: string = globalThis.__CHEMISTRY_INSTRUCTIONS__ ?? '';

declare global {
  // eslint-disable-next-line no-var
  var __CHEMISTRY_INSTRUCTIONS__: string | undefined;
}
